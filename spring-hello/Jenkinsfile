pipeline {
    agent any

    environment {
        TEST_CONTAINER = 'spring-hello-container'
        PROD_CONTAINER = 'spring-prod-container'
        INTERNAL_PORT = '8086'
        TEST_PORT = '9090'
        PROD_PORT = '9091'
        BASE_URL = "http://localhost:${TEST_PORT}"
    }

    stages {
        stage('Build Spring Boot App') {
            steps {
                dir('spring-hello') {
                    echo "📦 Building Spring Boot application..."
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Start Test Container') {
            steps {
                script {
                    echo "🐳 Starting test container ${TEST_CONTAINER}..."
                    def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker start ${TEST_CONTAINER}"
                        echo "✅ Reused existing test container."
                    } else {
                        dir('spring-hello') {
                            echo "📦 Building Docker image..."
                            bat "docker build -t spring-hello:latest ."
                        }
                        bat "docker create --name ${TEST_CONTAINER} -p ${TEST_PORT}:${INTERNAL_PORT} spring-hello:latest"
                        bat "docker start ${TEST_CONTAINER}"
                        echo "🚀 New test container started."
                    }
                    sleep time: 20, unit: 'SECONDS'
                }
            }
        }

        stage('Install Playwright') {
            steps {
                dir('spring-hello') {
                    echo "🧪 Installing Playwright..."
                    bat 'npm ci'
                    bat 'npx playwright install'
                }
            }
        }

        stage('Run E2E Tests') {
            steps {
                dir('spring-hello/playwright-tests') {
                    echo "🚦 Running Playwright E2E tests..."
                    script {
                        def testStatus = bat(
                            script: "set BASE_URL=${env.BASE_URL} && npx playwright test --reporter=html",
                            returnStatus: true
                        )
                        if (testStatus != 0) {
                            error("❌ Playwright tests failed. Stopping pipeline.")
                        }
                    }
                }
            }
        }

        stage('Publish HTML Report') {
            steps {
                echo "📊 Publishing Playwright test report..."
                script {
                    def reportExists = fileExists('spring-hello/playwright-tests/playwright-report/index.html')
                    if (!reportExists) {
                        error("❌ Playwright report not found. Check test execution.")
                    }
                }
                publishHTML(target: [
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true,
                    reportDir: 'spring-hello/playwright-tests/playwright-report',
                    reportFiles: 'index.html',
                    reportName: 'Playwright E2E Test Report',
                    reportTitles: 'Playwright Test Results'
                ])
            }
        }

        stage('Stop Test Container') {
            steps {
                echo "🛑 Stopping test container..."
                script {
                    def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker stop ${TEST_CONTAINER}"
                    }
                }
            }
        }

        stage('Deploy to Production') {
            steps {
                echo "🚀 Deploying to production..."
                script {
                    def exists = bat(script: "docker inspect --type container ${PROD_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker stop ${PROD_CONTAINER}"
                        bat "docker rm ${PROD_CONTAINER}"
                    }
                    bat "docker run -d --name ${PROD_CONTAINER} -p ${PROD_PORT}:${INTERNAL_PORT} spring-hello:latest"
                }
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline SUCCESS: App deployed to port ${PROD_PORT}."
        }
        failure {
            echo "❌ Pipeline FAILED. Cleaning up test container..."
            script {
                def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                if (exists == 0) {
                    bat "docker stop ${TEST_CONTAINER}"
                }
            }
        }
        always {
            echo '📦 CI/CD Pipeline finished.'
        }
    }
}
