pipeline {
    agent any

    environment {
        TEST_CONTAINER = 'spring-hello-container'
        PROD_CONTAINER = 'spring-prod-container'
        INTERNAL_PORT = '8086'
        TEST_PORT = '9090'
        PROD_PORT = '9091'
        BASE_TEST_URL = "http://localhost:${TEST_PORT}"
        BASE_PROD_URL = "http://localhost:${PROD_PORT}"
    }

    stages {
        stage('Build Spring Boot App') {
            steps {
                dir('spring-hello') {
                    echo "📦 Building Spring Boot application..."
                    bat 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Start Test Container') {
            steps {
                script {
                    echo "🐳 Starting test container..."
                    def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker start ${TEST_CONTAINER}"
                        echo "✅ Reused existing test container."
                    } else {
                        dir('spring-hello') {
                            bat "docker build -t spring-hello:latest ."
                        }
                        bat "docker create --name ${TEST_CONTAINER} -p ${TEST_PORT}:${INTERNAL_PORT} spring-hello:latest"
                        bat "docker start ${TEST_CONTAINER}"
                        echo "🚀 New test container started."
                    }
                    sleep time: 20, unit: 'SECONDS'
                }
            }
        }

        stage('Install Playwright') {
            steps {
                dir('spring-hello') {
                    echo "🧪 Installing Playwright..."
                    bat 'npm ci'
                    bat 'npx playwright install'
                }
            }
        }

        stage('Run E2E Tests (Test Container)') {
            steps {
                dir('spring-hello/playwright-tests') {
                    echo "🚦 Running Playwright E2E tests (Test)..."
                    bat 'rmdir /s /q playwright-report-test || echo No report to delete'
                    script {
                        def testStatus = bat(
                            script: """
                                set BASE_URL=${env.BASE_TEST_URL}
                                npx playwright test --reporter=html --project=chromium
                                move /Y playwright-report ..\\playwright-report-test
                            """,
                            returnStatus: true
                        )
                        if (testStatus != 0) {
                            error("❌ Playwright test against test container failed.")
                        }
                    }
                }
            }
        }

        stage('Publish Test Report') {
            steps {
                echo "📊 Publishing test report (Test)..."
                publishHTML(target: [
                    reportDir: 'spring-hello/playwright-report-test',
                    reportFiles: 'index.html',
                    reportName: 'Playwright E2E Report (Test)',
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true
                ])
            }
        }

        stage('Stop Test Container') {
            steps {
                echo "🛑 Stopping test container..."
                script {
                    def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker stop ${TEST_CONTAINER}"
                    }
                }
            }
        }

        stage('Deploy to Production') {
            steps {
                echo "🚀 Deploying to production..."
                script {
                    def exists = bat(script: "docker inspect --type container ${PROD_CONTAINER}", returnStatus: true)
                    if (exists == 0) {
                        bat "docker stop ${PROD_CONTAINER}"
                        echo "🔁 Restarting existing production container"
                    } else {
                        bat "docker run -d --name ${PROD_CONTAINER} -p ${PROD_PORT}:${INTERNAL_PORT} spring-hello:latest"
                        echo "🆕 New production container started"
                    }
                    bat "docker start ${PROD_CONTAINER}"
                    sleep time: 15, unit: 'SECONDS'
                }
            }
        }

        stage('Run E2E Tests (Production)') {
            steps {
                dir('spring-hello/playwright-tests') {
                    echo "🚦 Running Playwright E2E tests (Production)..."
                    bat 'rmdir /s /q playwright-report-prod || echo No prod report to delete'
                    script {
                        def prodTestStatus = bat(
                            script: """
                                set BASE_URL=${env.BASE_PROD_URL}
                                npx playwright test --reporter=html --project=chromium
                                move /Y playwright-report ..\\playwright-report-prod
                            """,
                            returnStatus: true
                        )
                        if (prodTestStatus != 0) {
                            error("❌ Playwright test against production failed.")
                        }
                    }
                }
            }
        }

        stage('Publish Test Report (Production)') {
            steps {
                echo "📊 Publishing test report (Production)..."
                publishHTML(target: [
                    reportDir: 'spring-hello/playwright-report-prod',
                    reportFiles: 'index.html',
                    reportName: 'Playwright E2E Report (Production)',
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true
                ])
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline SUCCESS: Application deployed and validated successfully."
        }
        failure {
            echo "❌ Pipeline FAILED. Cleaning up test container..."
            script {
                def exists = bat(script: "docker inspect --type container ${TEST_CONTAINER}", returnStatus: true)
                if (exists == 0) {
                    bat "docker stop ${TEST_CONTAINER}"
                }
            }
        }
        always {
            echo '📦 CI/CD Pipeline complete.'
        }
    }
}
